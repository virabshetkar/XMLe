name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore
        run: dotnet restore

  publish-nuget:
    runs-on: self-hosted

    steps:
      - name: Push to nuget
        run: dotnet nuget push nupkgs/*.nupkg -k ${{ secrets.GITHUB_TOKEN }} -s "https://nuget.pkg.github.com/virabshetkar/index.json"

  release:
    runs-on: self-hosted
    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v2
